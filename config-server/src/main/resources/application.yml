
server:
  port: 8889

spring:
  application:
    name: config-server


  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jahid-Iqbal/meow-mart-marketplace.git
          search-paths: config-server
          default-label: develop

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
